/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "friq0cw73yi1w3ka.public.blob.vercel-storage.com"
            }
        ]
    }
};

export default nextConfig;
